# 脚本执行关系说明

> **版本**: 2.1 | **最后更新**: 2026-01-29

## 📁 Python 脚本

| 脚本 | 用途 | 依赖 |
|------|------|------|
| `apihash_zero_detection.py` | ⭐ 主脚本 (日常使用) | apihash_lists |
| `apihash_lists.py` | API 列表模块 | dll_parser |
| `dll_parser.py` | DLL 解析工具 | pefile |
| `apihashreplace.py` | 原始脚本 (独立) | 无 |

---

## 🔗 依赖关系

```
apihash_zero_detection.py  ⭐ 主脚本
    ↓ imports
apihash_lists.py           📦 API 列表
    ↓ imports
dll_parser.py              🔧 DLL 解析
    ↓ reads/writes
dll_cache.json             💾 缓存

apihashreplace.py          📜 独立脚本
```

---

## 🚀 使用方式

### 首次设置

```bash
pip install pefile
python dll_parser.py -v
```

### 日常使用

```bash
# 推荐 (不含 CLD 移动)
python apihash_zero_detection.py -a x64 -i beacon.bin \
  --uppercase-dlls --transform-toupper

# 完整零检测
python apihash_zero_detection.py -a x64 -i beacon.bin --zero-detection
```

### 反汇编对比

```bash
pip install capstone
python shellcode_disasm.py beacon.bin beacon_modified.bin
```

---

## 📊 脚本功能

### apihash_zero_detection.py (主脚本)

**四层绕过**:
1. ✅ API 哈希随机化
2. ✅ DLL 名称大写化 (16 种 DLL)
3. ✅ CLD 指令移动 (只替换不插入)
4. ✅ ToUpper 函数变形 (YARA 绕过)

**参数**:
```bash
-a, --arch         架构 (x64/x86)
-i, --input        输入文件
-o, --output       输出文件
--zero-detection   完整绕过
--uppercase-dlls   DLL 大写化
--transform-toupper  ToUpper 变形
--evade-yara       YARA 绕过
-v, --verbose      详细输出
```

### dll_parser.py

**功能**: 解析 Windows DLL,提取导出函数

**支持的 DLL**:
- kernel32.dll, ntdll.dll, advapi32.dll
- ws2_32.dll, wininet.dll, winhttp.dll
- user32.dll, shell32.dll, 等 12 个

### shellcode_disasm.py

**功能**: 反汇编和对比 shellcode

```bash
# 单文件
python shellcode_disasm.py beacon.bin

# 对比
python shellcode_disasm.py beacon.bin beacon_modified.bin

# 分析
python shellcode_disasm.py beacon.bin --analyze
```

---

## ⚠️ 注意事项

### CLD 移动限制

- 只替换已有的 NOP,不插入
- 如果没有 NOP,会跳过
- 保证 shellcode 大小不变

### 大小验证

修改后大小必须与原始相同!如果不同说明有问题。

---

*最后更新: 2026-01-29*
